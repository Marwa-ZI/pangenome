name: pangenome-env
# Environnement pan-génome - SOLUTION FINALE
# QUAST installé via apt.txt (package Ubuntu)
# Roary installé via conda (sans conflit Perl)
# Kernels : Python + Bash (SANS kernel R)

channels:
  - conda-forge
  - bioconda
  - defaults

channel_priority: flexible

dependencies:
  # ============================================================================
  # JUPYTER ET KERNELS
  # ============================================================================
  - jupyterlab>=3.6,<5
  - jupyterhub>=3.0,<5
  - jupyter-resource-usage
  - notebook
  - nodejs>=18
  - ipykernel
  - ipywidgets>=8.0
  
  # ============================================================================
  # PYTHON SCIENTIFIQUE
  # ============================================================================
  - python=3.11
  - pip>=24.0
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - matplotlib>=3.7
  - seaborn>=0.12
  - biopython>=1.81
  - pysam>=0.21
  - scikit-learn>=1.3
  - plotly>=5.0
  
  # ============================================================================
  # R + PACKAGES (SANS KERNEL JUPYTER)
  # ============================================================================
  - r-base=4.3
  - r-tidyverse
  - r-ggplot2
  - r-readr
  - r-dplyr
  - r-tidyr
  - r-stringr
  - r-httr
  - r-jsonlite
  
  # ============================================================================
  # OUTILS BIOINFORMATIQUES
  # ============================================================================
  
  # Contrôle qualité
  - fastqc
  - multiqc
  
  # Trimming
  - trimmomatic
  
  # Mapping/SAM
  - bwa
  - samtools
  - htslib
  
  # Assemblage
  - spades
  
  # QUAST installé via apt.txt (package Ubuntu)
  # Pas de quast ici pour éviter conflit Perl avec Roary
  
  # Identification
  - blast
  - mlst
  
  # Annotation
  - prokka
  
  # Pan-génome
  - roary
  
  # Outils support
  - hmmer
  - cd-hit
  
  # Perl géré automatiquement par conda pour Prokka et Roary
  
  # ============================================================================
  # INSTALLATIONS VIA PIP
  # ============================================================================
  - pip:
    - bash_kernel
    - cyvcf2
    - watermark
